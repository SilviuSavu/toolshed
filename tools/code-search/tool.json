{
  "name": "code-search",
  "description": "Semantic and keyword code search across indexed repositories via Sourcegraph",
  "category": "code-intelligence",
  "type": "native",
  "max_output": 32768,
  "health": "code-search health",
  "tier": "cold",
  "commands": {
    "query": {
      "description": "Search code across all indexed repositories. Returns matching file paths and code snippets.",
      "args": {
        "query": {
          "type": "string",
          "required": true,
          "positional": true,
          "description": "Search query (supports Sourcegraph syntax: repo:, file:, lang: filters)"
        },
        "count": {
          "type": "string",
          "required": false,
          "positional": false,
          "description": "Maximum number of results (default: 30)"
        }
      }
    },
    "health": {
      "description": "Check Sourcegraph connectivity",
      "args": {}
    }
  }
}
